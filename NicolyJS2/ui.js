export function escolhapalpite() {
    return prompt("Digite um número entre 0 e 100:");
  }
  
  export function exibirmensa(mensagem) {
    console.log(mensagem);
  }
  
  export function exibirtenta(tentativa, totalTentativas) {
    console.log(`Tentativa ${tentativa} de ${totalTentativas}`);
  }